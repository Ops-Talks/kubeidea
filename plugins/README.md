# First-party sample plugins

This directory contains sample plugin implementations demonstrating the
Kube-IDEA plugin SDK. Third-party plugins should register via Python
entry points under the `kubeidea.plugins` group in their own
`pyproject.toml`.
